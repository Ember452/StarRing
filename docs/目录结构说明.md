# StarRing Phase 1 — 目录结构说明

## 设计理念

采用 **src-layout 标准工程结构**（PEP 517/518，hatchling 构建后端），遵循：
- **src/app/** 源码隔离：业务代码在 `src/app/` 下，构建系统仅发现 `src/` 内的包，`data/`/`storage/` 等非源码目录不会被误判为 Python 包
- **api/config/core/models/schemas/db** 职责分离：路由 / 配置 / 异常 / ORM / 序列化 / 数据库各自独立
- **API 版本化**：`api/v1/` 预留未来版本升级空间
- **分层架构自顶向下**：接入 → 编排 → Agent → 基础（RAG/工具/模型/DB） → 持久（storage/data）
- **scripts/ tests/** 独立：运维脚本与测试套件在 src 外层，不污染业务代码

### 为什么用 src-layout

| 问题 | 解决 |
|------|------|
| 扁平布局 `data/`/`storage/` 被 setuptools 误判为顶层包 | src-layout 隔离非源码目录 |
| `uv sync` 报错 `Multiple top-level packages discovered` | 构建后端仅扫描 `src/` |
| 导入测试时可能引入根目录文件 | `pythonpath = ["src"]` 明确源码路径 |

---

## 完整目录树 (Phase 1 — MVP Demo)

```
StarRing/                           # 项目根目录
│
├── src/                            # ★ 源码根目录（src-layout）
│   └── app/                        # 应用主包（所有业务代码）
│       ├── __init__.py             # 包说明 + 分层架构总览
│       ├── main.py                 # FastAPI 应用入口 + lifespan 生命周期
│       │
│       ├── api/                    # ── 接入层 ── API 路由
│       │   ├── __init__.py         # 路由聚合说明
│       │   └── v1/                 # API v1 版本
│       │       ├── __init__.py     # v1 版本说明
│       │       ├── router.py       # 路由聚合器（include_router）
│       │       ├── tasks.py        # POST /api/v1/tasks, GET /api/v1/tasks/{id}
│       │       └── health.py       # GET /api/v1/health（健康检查）
│       │
│       ├── config/                 # ── 配置中心 ── .env 多环境配置
│       │   ├── __init__.py         # 配置中心说明（Phase 1/2/3 演进规划）
│       │   └── settings.py         # pydantic-settings 配置类
│       │
│       ├── core/                   # ── 横切层 ── 异常 / 中间件 / 安全
│       │   ├── __init__.py         # 横切关注点说明
│       │   └── exceptions.py       # 全局异常处理器 + 自定义业务异常
│       │
│       ├── orchestration/          # ── 编排层 ── LangGraph 流水线
│       │   ├── __init__.py         # 编排层说明
│       │   └── pipeline.py         # StateGraph 4 节点串行流水线
│       │
│       ├── agents/                 # ── Agent 层 ── 业务 Agent
│       │   ├── __init__.py         # Agent 集说明
│       │   ├── requirement.py      # 需求拆解 Agent
│       │   ├── coding.py           # 编码开发 Agent
│       │   ├── testing.py          # 测试 Agent
│       │   └── documentation.py    # 文档输出 Agent
│       │
│       ├── rag/                    # ── 基础层 ── RAG 检索增强生成
│       │   ├── __init__.py         # RAG 层说明
│       │   ├── client.py           # Chroma 连接管理 / Collection 创建
│       │   ├── indexer.py          # AST 解析 → 向量化 → Chroma 入库
│       │   └── retriever.py        # 语义检索接口
│       │
│       ├── models/                 # ── 基础层 ── SQLAlchemy ORM 模型
│       │   ├── __init__.py         # ORM 模型层说明
│       │   └── task.py             # Task / TaskNode ORM 映射
│       │
│       ├── schemas/                # ── 基础层 ── Pydantic 序列化模型
│       │   ├── __init__.py         # Schema 层说明
│       │   └── task.py             # TaskCreate / TaskResponse / TaskStatus
│       │
│       ├── db/                     # ── 基础层 ── 数据库核心
│       │   ├── __init__.py         # 数据库层说明
│       │   ├── session.py          # async engine + sessionmaker + get_db
│       │   └── migrations/         # Alembic 迁移目录（Phase 2 启用）
│       │       └── .gitkeep
│       │
│       └── utils/                  # ── 基础层 ── 工具函数
│           ├── __init__.py         # 工具层说明
│           ├── file_writer.py      # 文件写入
│           └── db_schema.py        # 数据库表结构查询
│
├── scripts/                        # 运维脚本
│   └── init_db.py                  # 数据库初始化（uv run init-db）
│
├── tests/                          # 测试套件
│   ├── __init__.py                 # 测试包说明
│   ├── conftest.py                 # pytest 全局 fixtures
│   └── test_main.py                # FastAPI 基础接口测试
│
├── storage/                        # 生成文件存储（持久层）
│   └── .gitkeep
│
├── data/                           # 运行时数据（持久层，已加入 .gitignore）
│   └── .gitkeep
│
├── main.py                         # 根级入口，兼容 `uvicorn main:app`
├── pyproject.toml                  # uv 项目配置（hatchling 构建后端）
├── uv.lock                         # 依赖锁定文件
├── .python-version                 # Python 3.13
├── .env                            # 环境变量
├── .gitignore                      # Git 忽略规则
├── test_main.http                  # VSCode REST Client 测试请求
└── docs/                           # 项目文档
    └── 目录结构说明.md              # 本文档
```

---

## 分层原则

```
┌───────────────────────────────────────────────┐
│  接入层  src/app/api/     路由 / 请求验证     │
├───────────────────────────────────────────────┤
│  编排层  src/app/orchestration/  LangGraph   │
├───────────────────────────────────────────────┤
│  Agent层 src/app/agents/   业务 Agent 实现     │
├──────────┬──────────┬─────────────────────────┤
│  基础层  │          │                         │
│  rag/    │ models/  │ schemas/  utils/  db/   │
├──────────┴──────────┴─────────────────────────┤
│  横向   config/ 配置  |  core/ 异常/安全      │
├───────────────────────────────────────────────┤
│  持久层  storage/ + data/                      │
└───────────────────────────────────────────────┘
```

### 依赖方向（核心原则）

1. **api/** → 调用 `orchestration/`，不直接访问 `agents/`
2. **orchestration/** → 调度 `agents/`，不关心 Agent 内部实现
3. **agents/** → 调用 `rag/` + `utils/`，不直接操作 `db/`
4. **models/ ↔ schemas/** → 互不依赖，由 `api/` 负责 ORM ↔ Pydantic 转换
5. **config/** → 被所有层引用，不引用任何层（与 core/ 并列，配置和异常分离）
6. **db/session.py** → 由 `api/` 通过 FastAPI Depends 注入，Agent 不持有连接

---

## 各模块角色与职责

### 接入层 `src/app/api/v1/`

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/v1/tasks` | POST | 提交自然语言需求 → 创建 Task → 触发编排流水线 |
| `/api/v1/tasks/{id}` | GET | 查询任务状态 + 输出文件列表 |
| `/api/v1/health` | GET | 服务存活 + DB/Chroma 连通性检查 |

### 编排层 `src/app/orchestration/`

| 要素 | 说明 |
|------|------|
| 技术 | LangGraph StateGraph |
| 节点 | requirement → coding → testing → documentation（串行 4 节点） |
| 状态 | TypedDict `TaskState` 在节点间传递 |
| Phase 1 限制 | 无 Checkpointer、无中断恢复、无并行 |

### Agent 层 `src/app/agents/`

| Agent | 输入 | 输出 |
|-------|------|------|
| `requirement.py` | 自然语言需求 | PRD + 表结构 + API 清单 |
| `coding.py` | PRD + RAG 上下文 | Java Controller/Service/Mapper/Entity |
| `testing.py` | API 清单 + 生成代码 | JUnit + MockMvc 测试代码 |
| `documentation.py` | 生成代码 | OpenAPI YAML + DB 设计 Markdown |

每个 Agent 是纯函数：`(TaskState) -> TaskState`，不持有状态。

### RAG 层 `src/app/rag/`

| 模块 | 职责 |
|------|------|
| `client.py` | Chroma 客户端初始化、Collection 管理 |
| `indexer.py` | tree-sitter AST 解析 → embedding 向量化 → Chroma 入库 |
| `retriever.py` | 语义查询 → Top-K 相似代码片段 |

### 数据模型层 `src/app/models/` + `src/app/schemas/`

| 层 | 类型 | 用途 |
|----|------|------|
| `models/` | SQLAlchemy ORM | 数据库表映射（task, task_node） |
| `schemas/` | Pydantic BaseModel | API 请求/响应 JSON Schema |

分离原因：ORM 模型含数据库字段约束，API Schema 含验证和文档。混合会导致单一职责破坏。

---

## 依赖清单

### 已安装

| 包 | 用途 |
|---|------|
| `fastapi` | Web 框架 |
| `uvicorn` | ASGI 服务器 |

### 待安装（`uv add`）

| 包 | 用途 |
|---|------|
| `langgraph` | Agent 流水线编排 |
| `chromadb` | 向量数据库 |
| `openai` | SiliconFlow API 调用（兼容 OpenAI SDK） |
| `sqlalchemy` | ORM |
| `aiosqlite` | SQLite 异步驱动 |
| `python-dotenv` | .env 环境变量加载 |
| `pydantic-settings` | 类型安全配置管理 |
| `tree-sitter` | 代码 AST 解析核心 |
| `tree-sitter-java` | Java 语法解析 |
| `javalang` | Java 代码解析备选 |

```bash
cd StarRing
uv add langgraph chromadb openai sqlalchemy aiosqlite python-dotenv pydantic-settings tree-sitter tree-sitter-java javalang
```

---

## 构建系统

使用 **hatchling** 构建后端（比 setuptools 更简洁，原生支持 src-layout）：

```toml
# pyproject.toml
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/app"]
```

`uv sync --all-packages` 不再报 `Multiple top-level packages` 错误。

---

## 启动流程

```bash
# 1. 安装依赖
cd StarRing && uv sync

# 2. 配置 API Key
# 编辑 .env, 填入 SILICONFLOW_API_KEY

# 3. 初始化数据库
uv run python scripts/init_db.py

# 4. 启动服务
uv run uvicorn main:app --reload --host 0.0.0.0 --port 8000

# 5. 访问
# Swagger UI:  http://localhost:8000/docs
# ReDoc:       http://localhost:8000/redoc
# Health:      http://localhost:8000/api/v1/health
# REST Client: 使用 test_main.http 发送请求

# 6. 运行测试
uv run pytest tests/ -v
```

---

*基于 Phase 1 设计文档：[docs/phase-1-mvp-demo/README.md](../docs/phase-1-mvp-demo/README.md)*